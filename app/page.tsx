import Navbar from "./_components/navbar";

export default async function Home() {
  // const { userId } = await auth();

  // if (!userId) {
  //   redirect("/login");
  // }

  return (
    <>
      <Navbar />
    </>
  );
}
